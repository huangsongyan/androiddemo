<resources>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item> <!--toolbar颜色-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item><!--5.0以上状态栏颜色-->
        <item name="colorAccent">@color/colorAccent</item>
       <!-- <item name="android:textColorPrimary">@android:color/black</item>--> <!--toolbar title颜色-->
       <!-- <item name="android:windowBackground">@android:color/black</item>--> <!--窗口颜色-->
    </style>

    <style name="AppTheme" parent="@style/BaseAppTheme"/>

</resources>
